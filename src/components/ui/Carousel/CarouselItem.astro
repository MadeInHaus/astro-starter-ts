---
import type { HTMLTag, Polymorphic } from 'astro/types';

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag; className: string }>;

const { as: Tag, className } = Astro.props;
---

<Tag class={className}>
    <slot />
</Tag>
